<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>app.owlcms</groupId>
        <artifactId>owlcms4top</artifactId>
        <version>4.9.0.1</version>
    </parent>

    <artifactId>publicresults-windows</artifactId>
    <version>${revision}</version>

    <dependencies>
        <dependency>
            <groupId>app.owlcms</groupId>
            <artifactId>installtools</artifactId>
            <version>1.1.0</version>
            <type>zip</type>
            <classifier>jre</classifier>
        </dependency>
        <dependency>
            <groupId>app.owlcms</groupId>
            <artifactId>installtools</artifactId>
            <version>1.1.0</version>
            <type>zip</type>
            <classifier>innosetup</classifier>
        </dependency>
        <dependency>
            <groupId>app.owlcms</groupId>
            <artifactId>publicresults</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <!-- https://mvnrepository.com/artifact/com.coderplus.maven.plugins/copy-rename-maven-plugin -->
                <plugin>
                    <groupId>com.coderplus.maven.plugins</groupId>
                    <artifactId>copy-rename-maven-plugin</artifactId>
                    <version>1.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.1.2</version>
                <executions>
                    <execution>
                        <id>unpack-installtools</id>
                        <phase>package</phase>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeGroupIds>app.owlcms</includeGroupIds>
                            <includeArtifactIds>installtools</includeArtifactIds>
                            <outputDirectory>${project.build.directory}/installtools</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-files</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>app.owlcms</groupId>
                                    <artifactId>publicresults</artifactId>
                                    <outputDirectory>${project.build.directory}/publicresults-win32</outputDirectory>
                                    <destFileName>publicresults.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>extract-local</id>
                        <phase>package</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>app.owlcms</groupId>
                                    <artifactId>publicresults</artifactId>
                                    <outputDirectory>${project.build.directory}/publicresults-win32/local</outputDirectory>
                                    <includes>i18n**/**,templates**/**,config**/**,styles**/**</includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>com.akathist.maven.plugins.launch4j</groupId>
                <artifactId>launch4j-maven-plugin</artifactId>
                <version>2.1.1</version>
                <executions>
                    <execution>
                        <id>l4j</id>
                        <phase>package</phase>
                        <goals>
                            <goal>launch4j</goal>
                        </goals>
                        <configuration>
                            <headerType>console</headerType>
                            <outfile>${project.build.directory}/publicresults-win32/publicresults.exe</outfile>
                            <!-- fake jar; the one we actually run is in postCp below; we want the "local" directory to be seen before the 
                                publicresults jar we run from, but the plugin puts the value of jar at the beginning of classpath, which is not what we want. So we put a 
                                dummy jar at the front... -->
                            <!-- dummy jar for JDK 11 -->
                            <jar>jre/lib/jrt-fs.jar</jar>

                            <dontWrapJar>true</dontWrapJar>
                            <errTitle>publicresults</errTitle>
                            <classPath>
                                <mainClass>app.owlcms.publicresults.Main</mainClass>
                                <addDependencies>false</addDependencies>
                                <postCp>publicresults.jar</postCp>
                            </classPath>
                            <icon>src/main/install/owlcms.ico</icon>
                            <jre>
                                <minVersion>1.17.0</minVersion>
                                <path>jre</path>
                                <bundledJreAsFallback>false</bundledJreAsFallback>
                            </jre>
                            <versionInfo>
                                <fileVersion>1.0.0.0</fileVersion>
                                <txtFileVersion>${project.version}</txtFileVersion>
                                <fileDescription>${project.name}</fileDescription>
                                <copyright>Jean-Francois Lamy</copyright>
                                <productVersion>1.0.0.0</productVersion>
                                <txtProductVersion>1.0.0.0</txtProductVersion>
                                <productName>${project.name}</productName>
                                <companyName>...</companyName>
                                <internalName>publicresults</internalName>
                                <originalFilename>publicresults.exe</originalFilename>
                            </versionInfo>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- demo setup: the only difference is the name of the outfile -->
                        <id>l4j-demo</id>
                        <phase>package</phase>
                        <goals>
                            <goal>launch4j</goal>
                        </goals>
                        <configuration>
                            <headerType>console</headerType>
                            <outfile>${project.build.directory}/publicresults-win32/demo-publicresults.exe</outfile>
                            <!-- fake jar; the one we actually run is in postCp below; we want the "local" directory to be seen before the 
                                publicresults jar we run from, but the plugin puts the value of jar at the beginning of classpath, which is not what we want. So we put a 
                                dummy jar at the front... -->
                            <!-- dummy jar for JDK 11 -->
                            <jar>jre/lib/jrt-fs.jar</jar>

                            <dontWrapJar>true</dontWrapJar>
                            <errTitle>publicresults</errTitle>
                            <classPath>
                                <mainClass>app.owlcms.publicresults.Main</mainClass>
                                <addDependencies>false</addDependencies>
                                <postCp>publicresults.jar</postCp>
                            </classPath>
                            <icon>src/main/install/owlcms.ico</icon>
                            <jre>
                                <minVersion>1.17.0</minVersion>
                                <path>jre</path>
                                <bundledJreAsFallback>false</bundledJreAsFallback>
                            </jre>
                            <versionInfo>
                                <fileVersion>1.0.0.0</fileVersion>
                                <txtFileVersion>${project.version}</txtFileVersion>
                                <fileDescription>${project.name}</fileDescription>
                                <copyright>Jean-Francois Lamy</copyright>
                                <productVersion>1.0.0.0</productVersion>
                                <txtProductVersion>1.0.0.0</txtProductVersion>
                                <productName>${project.name}</productName>
                                <companyName>...</companyName>
                                <internalName>publicresults</internalName>
                                <originalFilename>publicresults.exe</originalFilename>
                            </versionInfo>
                        </configuration>
                    </execution>                    
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>windows_jdk</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <skipAssembly>${noInstaller}</skipAssembly>
                            <finalName>publicresults-win32</finalName>
                            <appendAssemblyId>false</appendAssemblyId>
                            <descriptors>
                                <descriptor>src/main/assembly/win32.xml</descriptor>
                            </descriptors>
                            <attach>false</attach>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>windows_jdk_package</id>
                        <phase>package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <skip>${noInstaller}</skip>
                            <executable>${project.basedir}/target/installtools/innosetup-portable-win32/app/ISCC.exe</executable>
                            <workingDirectory>${project.basedir}</workingDirectory>
                            <arguments>
                                <!-- argument>/Q</argument> -->
                                <argument>src\main\install\publicresults.iss</argument>
                            </arguments>
                            <environmentVariables>
                                <versionNumber>${project.version}</versionNumber>
                            </environmentVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            
                        <plugin>
                <groupId>com.coderplus.maven.plugins</groupId>
                <artifactId>copy-rename-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-prerelease</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <sourceFile>${project.build.directory}/publicresults_setup/publicresults_setup.exe</sourceFile>
                                    <destinationFile>${project.build.directory}/publicresults_setup/publicresults_setup_${project.version}.exe</destinationFile>
                                </fileSet>
                            </fileSets>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>signing</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>net.jsign</groupId>
                        <artifactId>jsign-maven-plugin</artifactId>
                        <version>2.1</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <phase>package</phase>
                                <configuration>
                                    <file>target/publicresults_setup/publicresults_setup.exe</file>
                                    <name>owlcms</name>
                                    <url>http://jflamy.github.io/owlcms4</url>
                                    <keystore>C:\Dev\Setup\owlcms_signing.pfx</keystore>
                                    <alias>te-e8b9a333-88b2-4fb6-83c9-4e0a2f8d1f11</alias>
                                    <storepass>${codeSigning}</storepass>
                                    <storetype>PKCS12</storetype>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>