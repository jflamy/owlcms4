<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>app.owlcms</groupId>
        <artifactId>owlcms4top</artifactId>
        <version>4.7.4.1</version>
    </parent>

    <name>Owlcms Docker packaging</name>
    <description>Owlcms Docker for use in Kubernetes</description>
    <artifactId>owlcms-docker</artifactId>
    <version>${revision}</version>
    <packaging>docker</packaging>

    <dependencies>
        <dependency>
            <groupId>app.owlcms</groupId>
            <artifactId>owlcms</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <execution>
                        <id>docker-tag</id>
                        <goals>
                            <goal>timestamp-property</goal>
                        </goals>
                        <configuration>
                            <name>dockerTag</name>
                            <pattern>yyyyMMddHHmmss</pattern>
                            <timeZone>America/Montreal</timeZone>
                            <locale>en,US</locale>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>docker-tag</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <finalName>docker-tag</finalName>
                            <appendAssemblyId>false</appendAssemblyId>
                            <descriptors>
                                <descriptor>src/main/assembly/docker-tag.xml</descriptor>
                            </descriptors>
                            <attach>false</attach>
                        </configuration>
                    </execution>
                    <execution>
                        <id>k8s</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <finalName>k8s</finalName>
                            <appendAssemblyId>false</appendAssemblyId>
                            <descriptors>
                                <descriptor>src/main/assembly/k8s.xml</descriptor>
                            </descriptors>
                            <attach>false</attach>
                            <filters>target/docker-tag/docker-tag.properties</filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>io.fabric8</groupId>
                <artifactId>docker-maven-plugin</artifactId>
                <version>0.33.0</version>
                <extensions>true</extensions>
                <configuration>
                    <skipRun>true</skipRun>
                    <images>
                        <image>
                            <name>owlcms:${dockerTag}</name>
                            <build>
                                <tags>
                                    <tag>latest</tag>
                                    <tag>${project.version}</tag>
                                </tags>
                                <from>adoptopenjdk/openjdk8:latest</from>
                                <assembly>
                                    <descriptor>docker.xml</descriptor>
                                </assembly>
                                <!-- monitoring requires a) opening a port. If running kubectl on WSL, and VisualVM on Windows, you need 
                                    to tell kubectl to listen on the real IP address that Windows can reach -->
                                <!-- kubectl port-forward (dash)(dash)address localhost,172.28.119.147 owlcms-76c8b879cc-t46 1088 -->
                                <!-- b) Then in VisualVM, use the add JMX Connection option, with the following string -->
                                <!-- service:jmx:rmi:///jndi/rmi://172.28.119.147:1088/jmxrmi -->
                                <entryPoint>
                                    <arg>java</arg>
                                    <arg>-Djava.net.preferIPv4Stack=true</arg>
                                    <arg>-Dcom.sun.management.jmxremote.ssl=false</arg>
                                    <arg>-Dcom.sun.management.jmxremote.authenticate=false</arg>
                                    <arg>-cp</arg>
                                    <arg>maven/classes:/maven/owlcms-${project.version}.jar</arg>
                                    <arg>app.owlcms.Main</arg>
                                </entryPoint>
                            </build>
                            <registry>owlcms-docker-containers.bintray.io</registry>
                        </image>
                    </images>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>