pipeline {
    agent any
    
    tools { 
        maven '3.6.2' 
        jdk 'jdk8' 
    }
    
    environment {
        // maven profile to select destination repository (release|prerelease)
        RELEASE = 'prerelease'
        M2 = 'c:/dev/.m2'
    }
                    
    stages {
        stage('Update git & submodules') {
            steps {
                bat 'git config --global user.email "jf@jflamy.dev"'
                bat 'git config --global user.name "Jean-François Lamy"'

                bat 'git pull origin develop'
                bat 'git checkout develop'
                bat 'git pull'
                bat 'git remote set-url origin https://jflamy@github.com/jflamy/owlcms4.git'

                dir('owlcms-publicresults/..') {
                    bat 'git remote set-url origin https://jflamy@github.com/jflamy/owlcms-publicresults.git'
                    // clone the submodules
                    bat 'git submodule update --init --recursive'
                    // don't trust the commit pointer, hard reset to develop and pull
                    bat 'git submodule foreach "git fetch && git reset --hard origin/develop && git checkout develop && git pull "'
                }
            }
        }
//        
//        stage('Build owlcms') {
//            steps {
//                dir('owlcms') {
//                    bat 'mvn -s %M2%/settings.xml -B clean package -P production,signing'
//                }
//            }
//        }
//        
//        stage('Build publicresults') {
//            steps {
//                dir('publicresults') {
//                    bat 'mvn -s %M2%/settings.xml -B clean install -P production'
//                }
//            }
//        }
//        
//        stage('Release publicresults jar to bintray') {
//            steps {
//                dir('publicresults') {
//                    bat 'mvn -s %M2%/settings.xml -B deploy:deploy-file@deploy-file'
//                }
//            }
//        }
//        
//       stage('Release owlcms-publicresults to Heroku') {
//            environment {
//                TOKEN = credentials('jflamy-pw')
//            }
//            steps {
//                dir('owlcms-publicresults') {
//                    bat 'mvn -s %M2%/settings.xml -B clean package'
//                    bat 'git pull'
//                    // the build does not create any files
//                    //bat 'git add -A'
//                    //bat 'git commit -m "successful build"'
//                    bat 'git checkout master'
//                    // bring back the updated pom with the correct version numbers
//                    bat 'git merge develop'
//                    // the build does not create any files
//                    //bat 'git commit -m "successful build merged to master"'
//                    bat 'git push https://%TOKEN%@github.com/jflamy/owlcms-publicresults.git'
//                }
//            }
//        }

        // keep this stage for last because gitflow:release updates the project version numbers
        stage('Release owlcms to github') {
            steps {
                dir('.') {
                    bat 'git checkout develop'
                    bat 'git pull'
                    bat 'mvn -s %M2%/settings.xml -B gitflow:release github-release:github-release -P %RELEASE%'
                }
            }
        }
        

    }
}